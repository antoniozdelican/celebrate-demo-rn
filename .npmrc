# @config-plugins/detox@11 (the latest release) still declares a peer of
# expo@^53 while this project runs SDK 57. The plugin's config mods work
# against the current Expo config-plugins API; only the declared range is
# stale. Without this, a plain `npm install` fails with ERESOLVE.
legacy-peer-deps=true
